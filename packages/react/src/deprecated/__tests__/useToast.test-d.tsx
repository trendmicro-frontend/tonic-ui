import { useToast } from '@tonic-ui/react';

// Basic usage (deprecated, use useToastManager)
function UseToastExample() {
  const toast = useToast();

  // Should return toast functions
  return null;
}
