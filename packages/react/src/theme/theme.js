import originalTheme from '@tonic-ui/theme';

const theme = {
  ...originalTheme,
  config: {
    prefix: 'tonic',
    useCSSVariables: false,
  },
  components: {},
  icons: [],
};

export default theme;
