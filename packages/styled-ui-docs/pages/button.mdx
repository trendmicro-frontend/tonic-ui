# Button

`Button` is used to trigger an action or event, such as submitting a form, opening a [`Modal`](./modal), canceling an action, or performing a delete operation.

## Import

```js
import Button from '@trendmicro/react-styled-ui/Button';
// or
import { Button } from '@trendmicro/react-styled-ui';
```

## Usage

```jsx
<Button>Default Button</Button>
```

### Variants

Use the `variant` prop to change the visual style of the Button. You can set the value to `emphasis`, `primary`, `default`, `secondary`, `ghost`.

```jsx
<Stack direction="row" spacing="2x">
  <Button variant="emphasis">Emphasis Button</Button>
  <Button variant="primary">Primary Button</Button>
  <Button variant="default">Default Button</Button>
  <Button variant="secondary">Secondary Button</Button>
  <Button variant="ghost">Ghost Button</Button>
</Stack>
```

The colors change based on `theme.colors`.

| variant | normal | hover | active | focus | disabled |
| :------ | :----- | :---- | :----- | :---- | :------- |
| emphasis | bg: `red:60` | bg: `red:50` | bg: `red:70` | bg: `blue:60` | bg: `gray:60` |
| primary | bg: `blue:60` | bg: `blue:50` | bg: `blue:70` | bg: `blue:60` | bg: `gray:60` |
| default | bg: `gray:60` | bg: `gray:50` | bg: `gray:70` | bg: `blue:60` | bg: `gray:60` |
| secondary | border: `gray:60` | border: `blue:50`, <br />font: `blue:40`  | border: `blue:50`, <br />font: `blue:40` | border: `blue:60` | border: `gray:60` |
| ghost | | border: `blue:50`, <br />font: `blue:40` | border: `blue:50`, <br />font: `blue:40` | border: `blue:60` | border: `gray:60` |


### Sizes

Use the `size` prop to change the size of the `Button`. You can set the value to `sm`, `md`, or `lg`.

```jsx
<Stack direction="column" spacing="4x">
  <Stack direction="row" spacing="2x" alignItems="center">
    <Button size="sm">Small</Button>
    <Button size="md">Medium</Button>
    <Button size="lg">Large</Button>
  </Stack>
  <Stack direction="row" spacing="2x" alignItems="center">
    <Button variant="secondary" size="sm">Small</Button>
    <Button variant="secondary" size="md">Medium</Button>
    <Button variant="secondary" size="lg">Large</Button>
  </Stack>
</Stack>
```

### States

```jsx
<Stack direction="column" spacing="4x">
  <Stack direction="row" spacing="2x" alignItems="center">
    <Button selected>Selected</Button>
    <Button>Normal</Button>
    <Button disabled>Disabled</Button>
  </Stack>
  <Stack direction="row" spacing="2x" alignItems="center">
    <Button variant="secondary" selected>Selected</Button>
    <Button variant="secondary">Normal</Button>
    <Button variant="secondary" disabled>Disabled</Button>
  </Stack>
</Stack>
```

### Icon buttons

Icon buttons are commonly used in toolbars. Sometimes you might want to add label to certain icon buttons to enhance the UX. You can use [Space](./space) to add a padding space with `.5rem` (`8px`) between icon and label.

```jsx
<Stack direction="column" spacing="4x">
  <Stack direction="row" spacing="2x">
    <Button variant="primary" width="8x" height="8x" px="0"  borderRadius="circle">
      <TMIcon name="add" />
    </Button>
    <Button variant="primary" width="8x" height="8x" px="0">
      <TMIcon name="add" />
    </Button>
    <Button variant="primary" borderRadius="2rem">
      <TMIcon name="add" />
      <Space width=".5rem" />
      Add Account
    </Button>
    <Button variant="primary">
      <TMIcon name="add" />
      <Space width=".5rem" />
      Add Account
    </Button>
  </Stack>
  <Stack direction="row" spacing="2x">
    <Button variantColor="blue" borderRadius="circle" width="8x" height="8x">
      <TMIcon name="add" color="white" />
    </Button>
    <Button variantColor="blue" width="8x" height="8x">
      <TMIcon name="add" color="white" />
    </Button>
    <Button variantColor="blue" borderRadius="2rem">
      <TMIcon name="add" color="white"/>
      <Space width=".5rem" />
      Add Account
    </Button>
    <Button variantColor="blue">
      <TMIcon name="add" color="white"/>
      <Space width=".5rem" />
      Add Account
    </Button>
  </Stack>
  <Stack direction="row" spacing="2x">
    <Button variant="secondary" borderRadius="circle" width="8x" height="8x">
      <TMIcon name="add" />
    </Button>
    <Button variant="secondary" width="8x" height="8x">
      <TMIcon name="add" />
    </Button>
    <Button variant="secondary" borderRadius="2rem">
      <TMIcon name="add" />
      <Space width=".5rem" />
      Add Account
    </Button>
    <Button variant="secondary">
      <TMIcon name="add" />
      <Space width=".5rem" />
      Add Account
    </Button>
  </Stack>
  <Stack direction="row" spacing="2x">
    <Button variant="ghost" borderRadius="circle" width="8x" height="8x">
      <TMIcon name="add" />
    </Button>
    <Button variant="ghost" width="8x" height="8x">
      <TMIcon name="add" />
    </Button>
    <Button variant="ghost" borderRadius="2rem">
      <TMIcon name="add" />
      <Space width=".5rem" />
      Add Account
    </Button>
    <Button variant="ghost">
      <TMIcon name="add" />
      <Space width=".5rem" />
      Add Account
    </Button>
  </Stack>
</Stack>
```

## Custom button

In case you need to customize the button, pass style props to the [ButtonBase](./buttonbase) component to create a custom button.

```jsx
<ButtonBase
  bg="#f5f6f7"
  border="1"
  borderColor="#ccd0d5"
  borderRadius="md"
  color="#4b4f56"
  fontSize="sm"
  fontWeight="semibold"
  height="24px"
  lineHeight="sm"
  px="2x"
  transition="all 0.2s cubic-bezier(.08,.52,.52,1)"
  _hover={{
    bg: "#ebedf0"
  }}
  _active={{
    bg: "#dddfe2",
    transform: "scale(0.98)",
    borderColor: "#bec3c9",
  }}
  _focus={{
    boxShadow:
      "0 0 1px 2px rgba(88, 144, 255, .75), 0 1px 1px rgba(0, 0, 0, .15)",
  }}
>
  Join Group
</ButtonBase>
```

## Props

`Button` composes the [`ButtonBase`](./buttonbase) component. You can override the default styles with style props.

| Name | Type | Default | Description |
| :--- | :--- | :------ | :---------- |
| disabled | boolean | | If `true`, the button will be disabled. This sets `aria-disabled=true` and you can style this state by passing the `_disabled` prop. |
| selected | boolean | | If `true`, it indicates that the button is in a selected state. |
| size | string | 'md' | The size of the button. One of: `'sm'`, `'md'`, `'lg'` |
| variant | string | 'default' | The variant of the button style to use. One of: `'emphasis'`, `'primary'`, `'default'`, `'secondary'`, `'ghost'` |
