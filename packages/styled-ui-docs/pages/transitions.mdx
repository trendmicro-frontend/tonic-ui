# Transitions

The Transition components use the `react-transition-group` package to perform animation effects and manage component states (including mounting and unmounting) over time.

## Import

```js
import {
  Collapse,
  Fade,
  Scale,
  Slide,
  Zoom,
} from '@trendmicro/react-styled-ui';
```

* The Collapse transition can be used for the Accordion component.
* The Fade transition can be used for the Modal component.
* The Scale transition can be used for the Tooltip and Popover components.
* The Slide transition can be used for the Drawer component.
* The Zoom transition can be used for zooming in/out the displayed components.

## Usage

### Collapse

Expand vertically from the top of the children. The `collapsedHeight` property can be used to set the minimum height when collapsed.

```jsx
function Example() {
  const { isOpen, onToggle } = useDisclosure();

  return (
    <>
      <Button onClick={onToggle} mb="4x">
        Toggle
      </Button>
      <Collapse in={isOpen} collapsedHeight={0}>
        <SkeletonContent>
          <SkeletonBody />
        </SkeletonContent>
      </Collapse>
    </>
  );
}
```

### Fade

```jsx
function Example() {
  const { isOpen, onToggle } = useDisclosure();

  return (
    <>
      <Button onClick={onToggle} mb="4x">
        Toggle
      </Button>
      <Fade in={isOpen} unmountOnExit>
        <SkeletonContent>
          <SkeletonBody />
        </SkeletonContent>
      </Fade>
    </>
  );
}
```

### Scale

```jsx
function Example() {
  const { isOpen, onToggle } = useDisclosure();

  return (
    <>
      <Button onClick={onToggle} mb="4x">
        Toggle
      </Button>
      <Scale in={isOpen} unmountOnExit>
        <SkeletonContent>
          <SkeletonBody />
        </SkeletonContent>
      </Scale>
    </>
  );
}
```

### Slide

```jsx noInline
const useSelection = (defaultValue) => {
  const [value, setValue] = React.useState(defaultValue);
  const changeBy = (value) => () => setValue(value);
  return [value, changeBy];
};

function Example() {
  const { isOpen, onClose, onToggle } = useDisclosure();
  const [direction, changeDirectionBy] = useSelection('up');

  return (
    <>
      <Box mb="4x">
        <Button onClick={onToggle}>
          Toggle
        </Button>
      </Box>
      <ButtonGroup
        variant="secondary"
        css={{
          '> *:not(:first-of-type)': {
            marginLeft: -1
          }
        }}
      >
        {['up', 'down', 'left', 'right'].map(value => {
          const changeDirection = changeDirectionBy(value);
          const onClick = () => {
            changeDirection();
            onClose();
          };

          return (
            <SelectableButton
              key={value}
              selected={value === direction}
              onClick={onClick}
              minWidth="15x"
            >
              {value}
            </SelectableButton>
          );
        })}
      </ButtonGroup>
      <Slide in={isOpen} direction={direction} unmountOnExit>
        <SkeletonContent mt="4x">
          <SkeletonBody />
        </SkeletonContent>
      </Slide>
    </>
  );
}

render(<Example />);
```

### Zoom

```jsx
function Example() {
  const { isOpen, onToggle } = useDisclosure();

  return (
    <>
      <Button onClick={onToggle} mb="4x">
        Toggle
      </Button>
      <Zoom in={isOpen} unmountOnExit>
        <SkeletonContent>
          <SkeletonBody />
        </SkeletonContent>
      </Zoom>
    </>
  );
}
```

## Props

### Fade

| Name | Type | Default | Description |
| :--- | :--- | :------ | :---------- |
| in | boolean | | Toggle visibility with a transition effect. |

### Scale

| Name | Type | Default | Description |
| :--- | :--- | :------ | :---------- |
| in | boolean | | Toggle visibility with a transition effect. |
| initialScale | number | 0.95 | The initial scale level. |

### Slide

| Name | Type | Default | Description |
| :--- | :--- | :------ | :---------- |
| in | boolean | | Toggle visibility with a transition effect. |
| direction | string | | One of: `'up'`, `'down'`, `'left'`, `'right'` |
