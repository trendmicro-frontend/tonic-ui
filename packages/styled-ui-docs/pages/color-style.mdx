import { colorStyle } from "@trendmicro/react-styled-ui";
import CodeBlock from "../components/CodeBlock";
import jsonPrettify from "../components/json-prettify";

# Color Style

Tonic Styled UI comes with a color style system that defines functional color values (See **Color Values** section below for details). You can override the default value by creating a color style object in accordance to the color mode, and pass the object to `ColorStyleProvider` with the `value` prop. For example:

```jsx disabled
import React from 'react';
import {
  ThemeProvider,
  ColorModeProvider,
  ColorStyleProvider,
  colorStyle,
} from '@trendmicro/react-styled-ui';
import App from './App';

const customColorStyle = {
  ...colorStyle,
   dark: {
    ...colorStyle.dark,
    text: {
      emphasis: 'white:primary',
      primary: 'white:primary',
      secondary: 'white:secondary',
      tertiary: 'white:tertiary',
      disabled: 'white:disabled',
      link: 'blue:40',
      warning: 'orange:50',
      error: 'red:50',
    },
  },
  light: {
    ...colorStyle.light,
    text: {
      emphasis: 'black:emphasis',
      primary: 'black:primary',
      secondary: 'black:secondary',
      tertiary: 'black:tertiary',
      disabled: 'black:disabled',
      link: 'blue:60',
      warning: 'orange:50',
      error: 'red:60',
    },
  },
};

function Example({ children }) {
  return (
    <ThemeProvider>
      <ColorModeProvider value="dark">
        <ColorStyleProvider value={customColorStyle}>
          <App />
        </ColorStyleProvider>
      </ColorModeProvider>
    </ThemeProvider>
  );
}
```

Then you can use the `useColorStyle` Hook to get or set color style for a given color mode.

```js
const [colorStyle, setColorStyle] = useColorStyle({ colorMode: 'dark' });

console.log(colorStyle.text.primary);
// => 'white:primary'
console.log(colorStyle.background.primary);
// => 'gray:100'
```

```jsx noInline
function Example() {
  const [colorMode] = useColorMode();
  const [colorStyle, setColorStyle] = useColorStyle({ colorMode });
  const backgroundColor = colorStyle.background.secondary;
  const textColor = colorStyle.text.secondary;
  const changeBackgroundColor = () => {
    setColorStyle(backgroundColorUpdater);
  };

  return (
    <>
      <Box mb="4x">
        <Button variant="primary" onClick={changeBackgroundColor}>
          Change background color
        </Button>
      </Box>
      <Box backgroundColor={backgroundColor} p="4x">
        <Text color={textColor} size="lg">
          You can change the color mode via the toggle button at the top right.
        </Text>
      </Box>
    </>
  );
};

const backgroundColorUpdater = (() => {
  const bgColors = ['red:50', 'magenta:50', 'purple:50', 'blue:50', 'green:50', 'teal:50', 'cyan:50', 'gray:50'];
  let bgIndex = 0;

  return (prevColorStyle) => {
    const nextColorStyle = {
      ...prevColorStyle,
      background: {
        ...prevColorStyle.background,
        secondary: bgColors[bgIndex],
      },
    };
    bgIndex = (bgIndex + 1) % bgColors.length;
    return nextColorStyle;
  };
})();

render(<Example />);
```

## Color Values

### Dark mode

<CodeBlock>
  {jsonPrettify(colorStyle.dark)}
</CodeBlock>

### Light mode

<CodeBlock>
  {jsonPrettify(colorStyle.light)}
</CodeBlock>

### Blindness mode

<CodeBlock>
  {jsonPrettify(colorStyle.blindness)}
</CodeBlock>
