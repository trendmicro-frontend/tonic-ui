# Menu

A menu displays a list of options to the user. It appears when the user interacts with a button, link, or other control that triggers an action.

## Import

- `Menu`: The Menu component provides context, state, and focus management for the menu.
- `MenuButton`: A button that can be used to open a menu.
- `MenuTrigger`: The trigger that opens the menu. This is usually a button or link.
- `MenuIndicator`: An indicator that appears next to the trigger. This is usually a chevron.
- `MenuList`: The wrapper for the menu items. Must be a direct child of `Menu`.
- `MenuItem`: The trigger that handles menu selection. Must be a direct child of a `MenuList`.
- `MenuGroup`: A wrapper to group related menu items.
- `MenuDivider`: A visual separator for menu items and groups.

```js
import {
  Menu,
  MenuButton,
  MenuTrigger,
  MenuIndicator,
  MenuList,
  MenuItem,
  MenuGroup,
  MenuDivider,
} from '@trendmicro/react-styled-ui';
```

## Usage

### Basic menu

A basic menu opens over the anchor element by default. The menu is closed when the user clicks outside of the menu.

```jsx
function Example() {
  const [anchorEl, setAnchorEl] = React.useState(null);
  const [isOpen, setIsOpen] = React.useState(false);
  const handleClick = (event) => {
    setAnchorEl(event.currentTarget);
    setIsOpen(!isOpen);
  };
  const handleClose = () => {
    setAnchorEl(null);
    setIsOpen(false);
  };

  return (
    <>
      <Button
        aria-controls="basic-menu"
        aria-haspopup="true"
        aria-expanded={isOpen ? 'true' : undefined}
        onClick={handleClick}
      >
        Settings
      </Button>
      <Menu
        id="basic-menu"
        anchorEl={anchorEl}
        isOpen={isOpen}
        onClose={handleClose}
      >
        <MenuList whiteSpace="nowrap">
          <MenuItem>
            <Icon icon="settings" mr="2x" />
            General
          </MenuItem>
          <MenuItem>
            <Icon icon="user-team" mr="2x" />
            Accounts
          </MenuItem>
          <MenuItem>
            <Icon icon="lock" mr="2x" />
            Privacy
          </MenuItem>
          <MenuDivider />
          <MenuItem disabled>
            <Icon icon="undo" mr="2x" />
            Restore Defaults
          </MenuItem>
        </MenuList>
      </Menu>
    </>
  );
}
```

### MenuGroup

To group related menu items, wrap them in a `MenuGroup` and pass a `title` prop for the group title.

```jsx noInline
const Avatar = (props) => (
  <Flex
    p="1x"
    borderRadius="50%"
    alignItems="center"
    justifyContent="center"
    fontSize="xs"
    lineHeight="1"
    {...props}
  />
);

function Example() {
  const [anchorEl, setAnchorEl] = React.useState(null);
  const [isOpen, setIsOpen] = React.useState(false);
  const handleClick = (event) => {
    setAnchorEl(event.currentTarget);
    setIsOpen(!isOpen);
  };
  const handleClose = () => {
    setAnchorEl(null);
    setIsOpen(false);
  };

  return (
    <>
      <ButtonBase
        onClick={handleClick}
      >
        <Avatar color="white" bg="gray:50">
          <Icon icon="user-team" size="5x" />
        </Avatar>
      </ButtonBase>
      <Menu
        anchorEl={anchorEl}
        isOpen={isOpen}
        onClose={handleClose}
      >
        <MenuList whiteSpace="nowrap">
          <MenuItem>
            Profile
          </MenuItem>
          <MenuDivider />
          <MenuGroup
            title={(
              <Flex justify="space-between">
                <Text>Other profiles</Text>
                <Space minWidth="10x" />
                <ButtonBase title="Manage profiles">
                  <Icon icon="settings" size="3x" />
                </ButtonBase>
              </Flex>
            )}
          >
            <MenuItem px="4x">
              <Avatar color="white" bg="blue:50" mr="2x">
                JD
              </Avatar>
              John Doe
            </MenuItem>
            <MenuItem px="4x">
              <Avatar color="white" bg="gray:50" mr="2x">
                <Icon icon="user" size="3x" />
              </Avatar>
              Guest
            </MenuItem>
          </MenuGroup>
        </MenuList>
      </Menu>
    </>
  );
}

render(<Example />);
```

### MenuTrigger

`MenuTrigger` is the trigger that opens the menu when clicked. It is usually a button or link. An indicator can be added to the trigger to indicate the menu is open or closed.

```jsx
<Flex align="center">
  <Menu>
    <MenuTrigger>
      <Flex alignItems="center" userSelect="none">
        <Icon icon="settings" />
        <Space minWidth="2x" />
        <Text>Settings</Text>
      </Flex>
    </MenuTrigger>
    <MenuList whiteSpace="nowrap">
      <MenuItem>
        <Icon icon="settings" mr="2x" />
        General
      </MenuItem>
      <MenuItem>
        <Icon icon="user-team" mr="2x" />
        Accounts
      </MenuItem>
      <MenuItem>
        <Icon icon="lock" mr="2x" />
        Privacy
      </MenuItem>
    </MenuList>
  </Menu>
  <Divider
    variant="solid"
    orientation="vertical"
    height="5x"
    mx="4x"
  />
  <Menu>
    <MenuTrigger>
      <Flex alignItems="center" userSelect="none">
        <Icon icon="settings" />
        <Space minWidth="2x" />
        <Text>Settings</Text>
        <Space width="1x" />
        <MenuIndicator />
      </Flex>
    </MenuTrigger>
    <MenuList whiteSpace="nowrap">
      <MenuItem>
        <Icon icon="settings" mr="2x" />
        General
      </MenuItem>
      <MenuItem>
        <Icon icon="user-team" mr="2x" />
        Accounts
      </MenuItem>
      <MenuItem>
        <Icon icon="lock" mr="2x" />
        Privacy
      </MenuItem>
    </MenuList>
  </Menu>
  <Divider
    variant="solid"
    orientation="vertical"
    height="5x"
    mx="4x"
  />
  <Menu>
    <MenuTrigger>
      {({ getMenuTriggerProps }) => {
        return (
          <Button
            {...getMenuTriggerProps()}
            variant="secondary"
          >
            Settings
            <Space width="2x" />
            <MenuIndicator />
          </Button>
        );
      }}
    </MenuTrigger>
    <MenuList whiteSpace="nowrap">
      <MenuItem>
        <Icon icon="settings" mr="2x" />
        General
      </MenuItem>
      <MenuItem>
        <Icon icon="user-team" mr="2x" />
        Accounts
      </MenuItem>
      <MenuItem>
        <Icon icon="lock" mr="2x" />
        Privacy
      </MenuItem>
    </MenuList>
  </Menu>
</Flex>
```

### MenuButton

`MenuButton` composes `MenuTrigger` and `Button` to create a button that opens the menu. It's an useful alternative to `MenuTrigger` when the trigger is a button.

```jsx disabled
<MenuTrigger>
  {({ getMenuTriggerProps }) => {
    return (
      <Button {...getMenuTriggerProps()}>
        {children}
      </Button>
    );
  }}
</MenuTrigger>
```

```jsx
<Menu>
  <MenuButton>
    Settings
    <Space width="2x" />
    <MenuIndicator />
  </MenuButton>
  <MenuList whiteSpace="nowrap">
    <MenuItem>
      <Icon icon="settings" mr="2x" />
      General
    </MenuItem>
    <MenuItem>
      <Icon icon="user-team" mr="2x" />
      Accounts
    </MenuItem>
    <MenuItem>
      <Icon icon="lock" mr="2x" />
      Privacy
    </MenuItem>
  </MenuList>
</Menu>
```

### Positioning

To position the menu relative to the trigger, pass a `placement` prop to `Menu`. It can be one of the following values: `top-start`, `top`, `top-end`, `right-start`, `right`, `right-end`, `bottom-start`, `bottom`, `bottom-end`, `left-start`, `left`, `left-end`.

```jsx
<Box display="inline-block">
  <Menu placement="top">
    <MenuButton variant="secondary">
      Select an option
      <Space width="1x" />
      <MenuIndicator />
    </MenuButton>
    <MenuList width="100%">
      <MenuItem>List item</MenuItem>
      <MenuItem>List item</MenuItem>
      <MenuItem>List item</MenuItem>
    </MenuList>
  </Menu>
</Box>
```

You can also add `skidding` or `distance` props to `MenuList` to control the distance between the menu and the trigger.

```jsx
<Menu>
  <MenuButton variant="secondary">
    Select an option
    <Space width="1x" />
    <MenuIndicator />
  </MenuButton>
  <MenuList
    distance={8}
    skidding={20}
  >
    <MenuItem>List item</MenuItem>
    <MenuItem>List item</MenuItem>
    <MenuItem>List item</MenuItem>
  </MenuList>
</Menu>
```

### MenuItem with checkbox

Set `closeOnSelect` to `false` to keep the menu open after selecting a menu item.

```jsx
function menuWithCheckbox() {
  const [checkedItems, setCheckedItems] = React.useState(['host', 'ip']);
  const handleChange = (e) => {
    const value = e.target.value;
    const items = [...checkedItems];
    items.indexOf(value) === -1 ? items.push(value) : items.splice(items.indexOf(value), 1);
    setCheckedItems(items);
  };
  return (
    <Menu closeOnSelect={false}>
      <MenuButton variant="secondary">
        Select an option
        <Space width="1x" />
        <MenuIndicator />
      </MenuButton>
      <MenuList whiteSpace="nowrap">
        <CheckboxGroup size="sm" defaultValue={checkedItems}>
          <MenuItem><Checkbox value="host" onChange={e => handleChange(e)}>Endpoint name</Checkbox></MenuItem>
          <MenuItem><Checkbox value="ip" onChange={e => handleChange(e)}>IP address</Checkbox></MenuItem>
          <MenuItem><Checkbox value="os" onChange={e => handleChange(e)}>Operating system</Checkbox></MenuItem>
        </CheckboxGroup>
      </MenuList>
    </Menu>
  );
}
```

## Accessibility

### Keyboard Interaction

| Key                | Action                                                                                                                                  |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------- |
| `Enter` or `Space` | When `MenuButton` receives focus, opens the menu and places focus on the first menu item                                                |
| `ArrowDown`        | When `MenuButton` receives focus, opens the menu and moves focus to the first menu item                                                 |
| `ArrowUp`          | When `MenuButton` receives focus, opens the menu and moves focus to the last menu item                                                  |
| `Escape`           | When the menu is open, closes the menu and sets focus to the `MenuButton`                                                               |

### ARIA roles

**For `MenuButton`:**

- It has `role` set to `button`.
- It has aria-haspopup set to either `menu`.
- When the menu is displayed, `MenuButton` has `aria-expanded` set to `true`.
- `MenuButton` has `aria-controls` set to the `id` of the `MenuList`.

**For `MenuList`:**

- It contains the `MenuItem` has role `menu`.

## Props

### Menu

| Name | Type | Default | Description |
| :--- | :--- | :------ | :---------- |
| children | ReactNode | | The children of the menu must be `MenuButton` or `MenuList` |
| isOpen | boolean | | If `true`, the menu will be opened |
| autoSelect | boolean | `false` | The menu will select the first enabled item when it opens, the trigger item must be `MenuButton` |
| closeOnBlur | boolean | `true` | If `true`, the menu will close on outside click or blur |
| closeOnSelect | boolean | `true` | If `true`, the menu will close on menu item select |
| placement | PopperJS.placement | `bottom-start` | The placement of the `MenuList`. One of: `'top'`, `'bottom'`, `'right'`, `'left'`, `'top-start'`, `'top-end'`, `'bottom-start'`, `'bottom-end'`, `'right-start'`, `'right-end'`, `'left-start'`, `'left-end'` |

### MenuButton

| Name | Type |
| :--- | :--- |
| children | ReactNode |
| onClick | React.MouseEventHandler |
| onKeyDown | React.KeyboardEventHandler |

### MenuTrigger

| Name | Type | Default | Description |
| :--- | :--- | :------ | :---------- |

### MenuIndicator

| Name | Type | Default | Description |
| :--- | :--- | :------ | :---------- |

### MenuList

| Name | Type | Default | Description |
| :--- | :--- | :------ | :---------- |
| children | ReactNode | | The content of the `MenuList`, should be the `MenuItem` component |
| onClick | React.MouseEventHandler | | Function to be called when you click on the menu item |
| onBlur | React.FocusEventHandler | | Function to be called when you blur out of the menu list |
| skidding | number | `0` | Displaces the menu (in pixels) along the reference element. Used by [`popper.js`](https://popper.js.org/docs/v2/modifiers/offset/#skidding-1) |
| distance | number | `0` | Displaces the menu (in pixels) away from, or toward, the reference. Used by [`popper.js`](https://popper.js.org/docs/v2/modifiers/offset/#distance-1) |

### MenuItem

| Name | Type | Default | Description |
| :--- | :--- | :------ | :---------- |
| disabled | boolean | If `true`, the menu item will be disabled |
| onClick | React.MouseEventHandler | Function that is called on click and enter/space keypress |
| onKeyDown | React.KeyboardEventHander | Function that is called on keydown |

### MenuGroup

| Name | Type | Default | Description |
| :--- | :--- | :------ | :---------- |
| children | ReactNode | The content of the menu group, should be the `MenuItem` component |
| title | string | The title of the menu group |
