import { Box, Stack } from "@tonic-ui/react";
import ThemeTokens from "@/components/ThemeTokens";

# shadows

## Design tokens

### Primitive tokens (v3)

<ThemeTokens type="legacy" scales={[{ name: 'shadows', defaultIsExpanded: true }]} />

### Semantic tokens

<ThemeTokens type="semantic" scales={[{ name: 'shadows', defaultIsExpanded: true }]} />


## Examples

{render('./shadows')}
