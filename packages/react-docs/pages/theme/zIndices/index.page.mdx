import { Box, Stack } from "@tonic-ui/react";
import ThemeTokens from "@/components/ThemeTokens";

# zIndices

## Design tokens

### Primitive tokens (v3)

<ThemeTokens type="primitive" scales={[{ name: 'zIndices', defaultIsExpanded: true }]} />

### Primitive tokens (v2)

<ThemeTokens type="legacy" scales={[{ name: 'zIndices', defaultIsExpanded: true }]} />

## Examples

{render('./zIndices')}
