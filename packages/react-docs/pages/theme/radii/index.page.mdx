import { Box, Stack } from "@tonic-ui/react";
import ThemeTokens from "@/components/ThemeTokens";

# radii

## Design tokens

### Primitive tokens (v3)

<ThemeTokens type="primitive" scales={[{ name: 'radii', defaultIsExpanded: true }]} />

### Primitive tokens (v2)

<ThemeTokens type="legacy" scales={[{ name: 'radii', defaultIsExpanded: true }]} />

## Examples

{render('./radii')}