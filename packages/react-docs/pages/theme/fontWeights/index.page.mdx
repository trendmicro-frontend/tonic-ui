import { Box, Stack } from "@tonic-ui/react";
import ThemeTokens from "@/components/ThemeTokens";

# fontWeights

## Design tokens

### Primitive tokens (v4)

<ThemeTokens type="primitive" scales={[{ name: 'fontWeights', defaultIsExpanded: true }]} />

### Primitive tokens (v3)

<ThemeTokens type="legacy" scales={[{ name: 'fontWeights', defaultIsExpanded: true }]} />

## Examples

{render('./fontWeights')}
