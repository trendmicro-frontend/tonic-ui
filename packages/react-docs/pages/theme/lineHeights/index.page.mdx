import { Box, Stack } from "@tonic-ui/react";
import ThemeTokens from "@/components/ThemeTokens";

# lineHeights

## Design tokens

### Primitive tokens (v3)

<ThemeTokens type="primitive" scales={[{ name: 'lineHeights', defaultIsExpanded: true }]} />

### Primitive tokens (v2)

<ThemeTokens type="legacy" scales={[{ name: 'lineHeights', defaultIsExpanded: true }]} />


## Examples

{render('./lineHeights')}
