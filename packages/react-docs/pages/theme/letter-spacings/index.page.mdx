import { Box, Stack } from "@tonic-ui/react";
import ThemeParser from "@/components/ThemeParser";

# Letter spacings

## Design tokens

No design tokens are currently defined for letter spacings.

<ThemeParser theme="letterSpacings" />
