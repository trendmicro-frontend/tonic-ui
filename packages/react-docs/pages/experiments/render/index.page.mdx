import CodeBlock from '@/components/CodeBlock';
import _Render_ from '!!raw-loader!@/experiments/render/Render';

# Render

The `Render` component is a utility for conditionally rendering UI content based on a boolean condition.

## Usage

### Render prop

Use the `children` render prop to display content when the condition evaluates to `true`.

{render('./render-prop')}

### Fallback

Use the `fallback` prop to render alternative content when the condition evaluates to `false`.

{render('./fallback')}

## Source

### Render

<CodeBlock code={_Render_} />
