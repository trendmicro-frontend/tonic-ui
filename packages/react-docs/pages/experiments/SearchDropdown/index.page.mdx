import CodeBlock from '@/components/CodeBlock';
import code from '!!raw-loader!@/components/SearchDropdown';

# SearchDropdown

A search dropdown component based on `Dropdown` and `SearchInput`, designed to render a menu with selectable options and handle selection logic through search functionality.

## Usage

### Basic configuration

Here's the simplest configuration to get started with the `SearchDropdown` component.

```jsx
<SearchDropdown
  onSelect={(option) => {
    setOption(option);
  }}
  options={[
    { value: 1, label: 'Option 1' },
    { value: 2, label: 'Option 2' },
  ]}
  renderContent={({ options, renderOptions, renderSearchInput }) => (
    <>
      <Box px="3x" mb="2x">
        {renderSearchInput()}
      </Box>
      <Scrollbar
        maxHeight={36 * 5}
        overflowY="visible"
      >
        {renderOptions(options)}
      </Scrollbar>
    </>
  )}
  renderOption={(option, { searchKeyword }) => {
    return option.label;
  }}
>
  {option.label}
</SearchDropdown>
```

### Advanced configuration

Here's a more advanced and commonly used configuration pattern:

#### Single-select dropdown

{render('./single-select-dropdown')}

#### Multi-select dropdown

{render('./multi-select-dropdown')}

## Props

Check out the [Dropdown](../../experiments/Dropdown) and [DropdownBase](../../experiments/DropdownBase) components for more details.

## Source

<CodeBlock code={code} />
