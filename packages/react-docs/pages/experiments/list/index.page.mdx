import CodeBlock from '@/components/CodeBlock';
import _List_ from '!!raw-loader!@/experiments/list/List';
import _ListItem_ from '!!raw-loader!@/experiments/list/ListItem';

# List

The `List` component is used to display a list of items. Each item is rendered with the `ListItem` component.

## Usage

{render('./usage')}

## Source

### List

<CodeBlock code={_List_} />

### ListItem

<CodeBlock code={_ListItem_} />
