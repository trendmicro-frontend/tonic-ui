import CodeBlock from '@/components/CodeBlock';
import rawList from '!!raw-loader!@/components/List';
import rawListItem from '!!raw-loader!@/components/ListItem';

# List

The `List` component is used to display a list of items. Each item is rendered with the `ListItem` component.

## Usage

{render('./usage')}

## Source

### List
<CodeBlock code={rawList} />

### ListItem
<CodeBlock code={rawListItem} />
