import CodeBlock from '@/components/CodeBlock';
import SourceCode from '@/components/SourceCode';
import { List, ListItem } from '@tonic-ui/react';
import _For_ from '!!raw-loader!@/experiments/for/For';

# For

The `For` component provides a declarative way to iterate over collections and render each item.

## Examples

### Object

Use the `For` component to loop over an object.

{render('./object')}

### Fallback

Use the `fallback` prop to render alternative content when the array is either empty or not provided (e.g. `undefined` or `null`).

{render('./fallback')}

## Source code

<List>
  <ListItem>
    <SourceCode name="For.js" code={_For_} />
  </ListItem>
</List>
