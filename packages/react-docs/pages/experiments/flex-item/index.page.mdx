import CodeBlock from '@/components/CodeBlock';
import _FlexItem_ from '!!raw-loader!@/experiments/flex-item/FlexItem';

# FlexItem
 
The `FlexItem` component is a child element in a flex layout. It automatically adjusts its size based on the available space. You can also make it fixed in size with the `fixed` prop.

## Usage

{render('./usage')}

## Source

### FlexItem

<CodeBlock code={_FlexItem_} />
