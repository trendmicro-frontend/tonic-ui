import AnimatedCubeDemo from '../../components/AnimatedCubeDemo';

# Box

The `Box` component serves as a wrapper component for most of the CSS utility needs. It allows you to control styling using style props to build a variety of components.

<AnimatedCubeDemo />

## Import

```js
import { Box } from '@tonic-ui/react';
```

## Usage

### The `as` prop

By default, the `Box` component renders a `div` element. There might be cases where you want to render a different element, you can use the `as` prop to do that.

```jsx
<Box
  as="button"
  backgroundColor="blue:70"
  color="white:primary"
  border="none"
  borderRadius="sm"
  cursor="pointer"
  px="2x"
  py="1x"
>
  Button
</Box>
```

### Using style props

See the [Styled Props](../styled-system/style-props) page to learn how to use style props.

```jsx
<Box
  backgroundColor="blue:50"
  color="black:primary"
  display="inline-flex"
  px="4x"
  py="3x"
  fontSize="xl"
  lineHeight="xl"
>
  My component
</Box>
```

### Using pseudo style props

See the [Pseudo Styled Props](../styled-system/pseudo-style-props) page to learn how to use pseudo style props.
