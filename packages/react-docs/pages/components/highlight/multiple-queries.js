import { Highlight } from '@tonic-ui/react';

const App = () => (
  <Highlight query={['search', 'find', 'match']}>
    Use this component to search for text, find specific words, or match patterns in your content.
  </Highlight>
);

export default App;
