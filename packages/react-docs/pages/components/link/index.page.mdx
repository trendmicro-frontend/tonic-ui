# Link

Links are accessible elements used primarily for navigation. This component is styled to resemble a hyperlink and semantically renders an `<a>`.

## Import

```js
import { Link } from '@tonic-ui/react';
```

## Usage

### Variants

The `Link` component supports two variants — `default` and `underline`. Use the `variant` prop to specify which variant to use.

#### `default`

{render('./variant-default')}

#### `underline`

{render('./variant-underline')}


### Attributes

#### `disabled`

The `disabled` attribute is not natively supported by the anchor (`<a>`) element. The `Link` component adds it for styling purposes only.

{render('./attribute-disabled')}

### Icon links

{render('./icon-links')}

### External links

{render('./external-links')}

## Props

### Link

| Name | Type | Default | Description |
| :--- | :--- | :------ | :---------- |
| children | ReactNode | | |
| disabled | boolean | | If `true`, the link will be disabled. This sets `aria-disabled=true` and you can style this state by using the `_disabled` prop. |
| onClick | function | | A callback called when the link is clicked. |
