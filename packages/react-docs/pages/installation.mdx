# Installation

Tonic UI is available as an [npm package](https://www.npmjs.com/package/@tonic-ui/react).

Inside your React project directory, install Tonic UI using one of the following commands:

```bash
npm install @tonic-ui/react
```

```bash
yarn add @tonic-ui/react
```
