# Calendar

## Import

```js
import {
  Calendar,
} from '@tonic-ui/react-lab';
```

## Usage

```jsx noInline
const FormGroup = (props) => (
  <Box mb="4x" {...props} />
);

const firstDayOfWeekOptions = [
  0, // Sunday
  1, // Monday
  2, // Tuesday
  3, // Wednesday
  4, // Thursday
  5, // Friday
  6, // Saturday
];

const useSelection = (defaultValue) => {
  const [value, setValue] = React.useState(defaultValue);
  const changeBy = (value) => () => setValue(value);
  return [value, changeBy];
};

const mapDateSelectionToDateObject = (dateSelection) => ({
  'today': new Date(),
  'none': null,
  'invalid': new Date(''),
}[dateSelection]);

render(() => {
  const today = useConst(() => new Date());
  const [dateSelection, changeDateSelectionBy] = useSelection('today');
  const [date, setDate] = React.useState(mapDateSelectionToDateObject(dateSelection));
  const [maxDate, setMaxDate] = React.useState('');
  const [minDate, setMinDate] = React.useState('');
  const [error, setError] = React.useState();
  const [firstDayOfWeek, changeWeekStartDayBy] = useSelection(0);
  const dateFormat = 'yyyy-MM-dd';
  const displayDate = dateFns.isValid(date) ? dateFns.format(date, dateFormat) : '';

  React.useEffect(() => {
    setDate(mapDateSelectionToDateObject(dateSelection));
  }, [dateSelection]);

  return (
    <>
      <Flex mb="4x" columnGap="2x">
        <Text>
          Selected date:
        </Text>
        {error && (
          <Text color="red:50">{error}</Text>
        )}
        {!error && (
          <Text>{displayDate}</Text>
        )}
      </Flex>
      <Calendar
        date={date}
        firstDayOfWeek={firstDayOfWeek}
        maxDate={maxDate ? new Date(maxDate) : undefined}
        minDate={minDate ? new Date(minDate) : undefined}
        onChange={(nextDate) => {
          console.log('onChange:', nextDate);
          setDate(nextDate);
        }}
        onError={(error, value) => {
          console.log('onError:', error);
          setError(error);
        }}
      />
      <Divider my="4x" />
      <Box mb="4x">
        <Text fontSize="lg" lineHeight="lg">
          Calendar props
        </Text>
      </Box>
      <FormGroup>
        <Box mb="2x">
          <TextLabel>
            {dateSelection === 'today' && `date={new Date()}`}
            {dateSelection === 'none' && `date={null}`}
            {dateSelection === 'invalid' && `date={new Date('')}`}
          </TextLabel>
        </Box>
        <ButtonGroup
          variant="secondary"
          css={{
            '> *:not(:first-of-type)': {
              marginLeft: -1
            }
          }}
        >
          {['today', 'none', 'invalid'].map(value => (
            <Button
              key={value}
              selected={value === dateSelection}
              onClick={changeDateSelectionBy(value)}
              minWidth="15x"
            >
              {value}
            </Button>
          ))}
        </ButtonGroup>
      </FormGroup>
      <FormGroup>
        <Box mb="2x">
          <TextLabel>
            minDate={minDate ? `{new Date('${minDate}'}}` : ''}
          </TextLabel>
        </Box>
        <Input
          type="date"
          value={minDate}
          onChange={(e) => setMinDate(e.target.value)}
          width="auto"
        />
      </FormGroup>
      <FormGroup>
        <Box mb="2x">
          <TextLabel>
            maxDate={maxDate ? `{new Date('${maxDate}'}}` : ''}
          </TextLabel>
        </Box>
        <Input
          type="date"
          value={maxDate}
          onChange={(e) => setMaxDate(e.target.value)}
          width="auto"
        />
      </FormGroup>
      <FormGroup>
        <Box mb="2x">
          <TextLabel>
            firstDayOfWeek
          </TextLabel>
        </Box>
        <ButtonGroup
          variant="secondary"
          css={{
            '> *:not(:first-of-type)': {
              marginLeft: -1
            }
          }}
        >
          {[0, 1, 2, 3, 4, 5, 6].map(value => (
            <Button
              key={value}
              selected={value === firstDayOfWeek}
              onClick={changeWeekStartDayBy(value)}
              minWidth="15x"
            >
              {value}
            </Button>
          ))}
        </ButtonGroup>
      </FormGroup>
    </>
  );
});
```

## Props

### Calendar

| Name | Type | Default | Description |
| :--- | :--- | :------ | :---------- |
| date | Date | | The selected date. |
| defaultDate | Date | | The default selected date. |
| firstDayOfWeek | number | 0 | The first day of the week.<br/>0 = Sunday<br/>1 = Monday<br/>2 = Tuesday<br/>3 = Wednesday<br/>4 = Thursday<br/>5 = Friday<br/>6 = Saturday |
| minDate | Date | | The minimum date that can be selected. |
| maxDate | Date | | The maximum date that can be selected. |
| onChange | (value) => void | | A callback called when the selected date changes. The value is a `Date` object for the selected date. |
| onError | (error, value) => void | | An error-first callback called when the date validation returns an error (or the date is valid after error). The error is a string and the value is a `Date` object for the selected date. |
