// Empty stub so the remote's index.html has a module to load. The real federated
// artifact (remoteEntry.js + mf-manifest.json) is emitted by @module-federation/vite
// from the `exposes` config, not from this file.
export {};
