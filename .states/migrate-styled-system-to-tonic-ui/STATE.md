# Loop: Migrate tonic-one's new styled-system engine (semantic-token mapping + CSS-variables API + resolveTheme) into tonic-ui, keeping tonic-ui's v3 token VALUES (restructured into the new format), not v4 content.
_PHASE 1 MERGED TO v2 (user 2026-06-27): PR #1166 is in origin/v2. So when `main`(v3) is cut from v2 it will ALREADY contain Phase 1 (Portal + env/shadow-dom docs + sidebar/llms fixes 5e9e88d/7757386). REBASE PLAN for Phase 2 onto origin/main: DROP the redundant Phase-1-derived commits still on the Phase 2 branch (Portal 73f3e75, docs-sync 191477e — content already in main via v2); KEEP only Phase 2's own work (MFE-feat 136a08dd, U2 aed56d2c, U4U5 0699c5a, U6-U10 ac436bc, U16 b200a8a, U14 44a0506, U15 theming-docs, + any later). Do NOT reintroduce the stale pre-fix docs-sync; main's v2-sourced sidebar/llms already correct. Likely an interactive rebase dropping 2 commits (or rebase --onto main <after-191477e>)._
_MERGE TARGET (user 2026-06-27): Phase 2 = "Tonic UI v3" work; merges to the `main` branch, which WILL be branched from `v2`. So the v2-derived base of feat/semantic-token-css-variables (merge-base = origin/v2 tip e40a370806) is CORRECT — v3 starts from v2. origin/main does NOT exist yet. ACTION when it does: rebase feat/semantic-token-css-variables onto origin/main and open the Phase 2 PR against `main` (NOT v2, NOT feat/environment-provider). Phase 1 commits (Portal 73f3e75, docs-sync 191477e + sidebar/llms fixes) currently ride on the Phase 2 branch; a rebase onto main will auto-drop them if Phase 1 already flowed v2→main, else they ride along. No engine rework needed — code targets v2 structure = v3 start._
_Stop when: all units done; tonic-ui `yarn test` + `yarn lint` green across react/react-base/styled-system/theme/utils; CSS-var `[data-color-scheme]` color-mode switching works with v3 values; Phase 2 PR opened (stacked on Phase 1)._
_Branch: feat/semantic-token-css-variables (tonic-ui, off feat/environment-provider)     Posture: supervised (yield at U2 design + any checker fail)     Budget (run): ~12 units_
_SEQUENCING: Phase 2 starts ONLY after Phase 1 (env+MFE loop) is finished. Parked until then._
_PHASE 1 COMPLETE (2026-06-27): feat/environment-provider tip 191477e33b (env-provider only; on origin/v2 + ButtonBox). Phase 2 branch RE-STACKED + already includes the MFE examples: feat/semantic-token-css-variables tip 136a08dd02 (= Phase 1 191477e + MFE-feat). First Phase 2 action: resolve the 10 token-mapping open questions above._
_MFE moved into Phase 2 (2026-06-27): MFE demos crash without the engine (`theme.get is not a function`) and reference v4 `colors._component.scrollbar.*` tokens. Phase 2 must (a) land the engine so `theme.get` exists, and (b) ensure the tokens the MFE GlobalStyles reference resolve under the v3-restructured theme (either provide `_component.scrollbar.*` equivalents or adapt the two GlobalStyles.jsx). Add unit U16: make MFE demos run live (wujie + module-federation) + browser-verify. The Phase 2 engine work (U2-U11) is the prerequisite._

## Repos
- SOURCE (the new engine, reference, DO NOT EDIT): `/home/cheton/Code/trend-common-platform/tonic-one`
- TARGET (edit here): `/home/cheton/Code/trendmicro-frontend/tonic-ui`, branch `feat/semantic-token-css-variables`
- NOTE: git output is rewritten by an `rtk` hook — prefix git with `rtk proxy` for raw output.

## Decisions (from user)
- Phase 1 (environment provider, incl. EnvironmentProvider #1088 + Portal getRootNode) Portal committed (1d5a0ecdf8). Phase 2 stacks on it; separate PR that follows the Phase 1 PR. (Phase 1 has MORE remaining work — see the separate loop `.states/migrate-environment-mfe-to-tonic-ui/`.)
- Strategy (REVISED 2026-06-27 — supersedes earlier "Activate with v3 values"): bring the ENGINE INFRASTRUCTURE only (resolveTheme/toColorMode, toCSSVariable, createTheme cssVariables, getter useCSSVariables+__colorMode path, Box __colorMode injection, useTheme.toColorMode+.get(), CSSVariables, providers) operating over the EXISTING v3 FLAT tokens. Do NOT re-express colorStyle into `_dark`/`_light` semantic tokens. v3 KEEPS using `colorStyle` to drive dark/light. The semantic `_dark`/`_light` tokens + `[data-color-scheme]` CSS-var color-mode activation are DEFERRED until v4 semantic tokens are integrated into the v4 theme — not this phase.
- KEEP v3 tokens and colorStyle UNTOUCHED (colon keys, flat strings). Engine is dormant w.r.t. semantic color-mode switching until v4.
- Net deliverables this phase: engine infra (esp. `theme.get` which also fixes the MFE crash) + CSS-variables API over flat v3 tokens + JSDoc/.d.ts + theming docs + MFE demos adapted to v3.
- SCOPE DECISION (2026-06-27, user): "Full CSS-vars API alignment now" — DO build U6-U10 (getter useCSSVariables+__colorMode, Box __colorMode, CSSVariables, useCSSVariables prop, color-mode data-color-scheme) over flat v3, even though semantic dark/light switching stays dormant until v4. HARD CONSTRAINT: default behavior (useCSSVariables OFF) must be byte-identical to current tonic-ui — NO component snapshot churn. theme.get already delivered (U4+U5, committed 0699c5a940).
- Out of scope: data-grid, charts, v4 token content.
- ADDED SCOPE (broader): introduce tonic-one's JSDoc typing system (`ForwardRefComponent` JSDoc typedefs) + the JSDoc→`.d.ts` generation pipeline, so tonic-ui can emit TypeScript definition files. tonic-ui currently ships NO .d.ts and has no dts plugin in rollup. This is its own subsystem — needs its own discovery + units (U13+). The styled-system engine units (U2-U12) and the JSDoc/d.ts units can proceed somewhat independently.
- Use git worktrees to separate Phase 1 (tonic-ui-p1 / feat/environment-provider) from Phase 2 (tonic-ui main checkout / feat/semantic-token-css-variables).

## Done
- [x] Phase 1 Portal delta committed (1d5a0ecdf8) — verified 11/11 portal tests green.
- [x] INTEGRATION RE-RUN on rebased base da482d087e (integration-checker, 2026-06-27): build PASS+idempotent (9 public pkgs, dts for 8; codemod JS-only by design), lint PASS (0 errors), docs build PASS (165/165 + MFE). TEST failures = PRE-EXISTING FLAKINESS only, NOT migration regressions (proven: engine branch git-diff touched neither menu/ nor date-pickers/). Caveat: run straddled E1 commit + E2 edits → does NOT certify current tree; FINAL pre-PR integration re-run MUST be on a FROZEN tree after all E tranches commit (no maker running).
- [x] U14-E1 high-traffic primitives (button×5, text×2, flex, grid, stack×2, link×2, icon) — JSDoc ported from tonic-one, props adapted to v3. types:build exit 0; jest 54/54 + 8 snap zero churn; lint 0 errors (13 React no-unused-vars warnings accepted — source-faithful, required by tsc). Committed b6e624c7ea.
- [x] U14-E2 form controls (input×8, textarea, checkbox×3, radio×3, switch, select×3, search-input×5, form-control×8, autocomplete×3 = 35 files; slider absent in tonic-ui) — props adapted (Select=outline|filled|unstyled; Input keeps flush). types:build exit 0; jest 14 suites/166 tests/9 snap zero churn; lint 0 errors (25 React warnings). Committed ee11674baf.
- [x] U14-E3 overlays/feedback (modal, drawer, popover, popper, portal, tooltip, toast, alert, spinner, progress, skeleton, badge, tag = 48 files; incl StyledFC cases popover/portal/toast) — types:build exit 0; lint 0 errors (43 React warnings); 10 snapshots zero churn. 1 batch jest failure (tooltip closeOnClick) = pre-existing popper/transition flakiness (passes 3/3 isolated; diff comment-only), NOT a regression. Committed e78836f31b.
- [x] U14-E4 navigation/structure (accordion, tabs, menu, table, tree, pagination, truncate, image, visually-hidden = 46 files; list already done, breadcrumb absent) — types:build exit 0; jest 15 suites/90 tests/8 snap zero churn (menu/Submenu green this run); lint 0 errors. Committed 094523aee0.
- [x] U14-E5a misc components (button-box, code, divider, highlight, mark, space, resize-handle, scrollbar×6, animate-presence, deprecated×4 = 19 files) — types:build exit 0; jest 15 suites/150 tests/9 snap zero churn; lint 0 errors (19 React warnings). Committed 7dae5f957b.
- [x] U14-E5b engine-touched + flaky (color-mode×4, provider/TonicProvider, theme/createTheme [ADAPTED to v3 engine sig], transitions×6, date-pickers×9 = 21 files) — all diffs comment/import-only (engine logic untouched). types:build exit 0; jest 15 suites/72 tests/1 snap zero churn (date-pickers green); lint 0 errors (17 React warnings). Committed 01dff8f4c3.
- U14 ANNOTATION COMPLETE: E1+E2+E3+E4+E5a+E5b = 14+35+48+46+19+21 = 183 component/hook files annotated (tonic-ui went from 2 → 185 ForwardRefComponent/typedef files). Source = tonic-one, all props adapted to v3.
- [x] FINAL FROZEN-TREE INTEGRATION GATE on 01dff8f4c3 (2026-06-27): build-public ×2 EXIT 0 (IDEMPOTENT, all public pkgs incl dts); react dist/index.d.ts now emits REAL types (6612 lines, 353 type refs — was loose/any pre-U14); lint EXIT 0 (0 errors, React warnings only); test 754/756 pass. The 2 failing suites = CONFIRMED flaky (DatePicker: failed 1/3 then passed 2/3; SubmenuToggle: passed 3/3 isolated) — pre-existing date-fns-v4/popper-timing family, NOT U14 regressions (all E diffs comment/import-only). BRANCH CERTIFIED READY for Phase 2 PR.

## In progress
- (none) — U14 annotation COMPLETE + certified. Only U12 Phase 2 PR remains (gated on user).

## U14-E5 split (computed from tonic-one annotated sources lacking tonic-ui annotation)
- E5a (LOW risk, ~20): button-box, code, divider, highlight, mark, space, resize-handle, scrollbar×6, utils/animate-presence/AnimatePresence, deprecated/{AccordionCollapse,Calendar}, menu/deprecated/SubmenuToggle, table/deprecated/{TableHeaderCell,TableHeaderRow}.
- E5b (HIGH risk — ENGINE-TOUCHED + FLAKY tests, ~21): color-mode/{ColorModeProvider,DarkMode,LightMode,InvertedMode}, provider/TonicProvider, theme/createTheme (engine-rewritten — adapt to tonic-ui's actual resolveTheme/toColorMode signature or FLAG), transitions/{Collapse,Fade,Grow,Scale,Slide,Zoom} (flaky family), date-pickers/* (KNOWN-FLAKY DatePicker — isolate-retry jest).
- NOTE: react-hooks PACKAGE had no annotated tonic-one sources matching the grep — no separate hooks tranche needed (the use* hooks in react/src dirs were annotated within E1/E2).

## Notes (env)
- Background Agent mechanism NON-FUNCTIONAL here (run_in_background agents never execute; task IDs untracked). Use FOREGROUND subagents only.
- VS Code Jest extension (orta.vscode-jest) runs jest in WATCH mode on the tonic-ui tree — expect auto re-runs/contention while makers edit; harmless (writes to /tmp, not repo).
- Integration re-run on rebased base: DONE on da482d087e (build/lint/docs GREEN) — see Done. FINAL re-run still needed on FROZEN tree after all E tranches commit (no maker editing), before the Phase 2 PR.
- PRE-EXISTING FLAKY TESTS (gate separately from this PR; NOT migration regressions, from date-fns v4 base #1159/#1158): `menu/__tests__/Submenu.test.js` (portal click) + `date-pickers/DatePicker/__tests__/DatePicker.test.js` (waitForElementToBeRemoved race). RELEVANT to E4 (menu/) scoped jest — re-run flaky failures in isolation before treating as a real E-tranche regression. date-pickers is NOT in any E tranche. ALSO flaky: `tooltip/__tests__/Tooltip.test.js` (closeOnClick, popper/transition timing) — confirmed flaky in E3 (passed 3/3 isolated). Pattern: popper+react-transition-group+portal suites flake under concurrent batch load; re-run isolated to confirm.
- Full-suite checks must NOT run concurrently with a maker editing the tree (isolation). Per-tranche scoped `jest --ci` is run only AFTER the maker settles, so it's fine.

## Next            ← ordered by dependency; pull from the top
### U14 Sub-unit E — JSDoc annotation tranches (ACTIVE; source = tonic-one)
- [ ] U14-E1 high-traffic primitives (~8): box (react-base — verify, may be done), button, text, flex, grid, stack, link, icon. Port `@typedef`+`@type {ForwardRefComponent}` from matching tonic-one files, adapt to tonic-ui props. done when: `yarn workspace @tonic-ui/react types:build` (+ react-base) exits 0, no dangling ForwardRefComponent ref in emitted dist/index.d.ts, JSDoc prop unions match the component's actual props/variants/defaults, and `yarn workspace @tonic-ui/react test` snapshots ZERO churn.
- [ ] U14-E2 form controls (~10): input, textarea, checkbox, radio, switch, select, search-input, form-control, autocomplete, slider/resize-handle. done when: same gate as E1, scoped to these dirs.
- [ ] U14-E3 overlays/feedback (~12): modal, drawer, popover, popper, portal, tooltip, toast, alert, spinner, progress, skeleton, badge/tag. (Popover/PortalManager/ToastTransitionGroup use `@type {StyledFC<...>}` not ForwardRefComponent — mirror tonic-one.) done when: same gate.
- [ ] U14-E4 navigation/structure (~12): accordion, tabs, menu, table, tree, pagination, breadcrumb, truncate, image, visually-hidden, + remaining misc dirs (list already done). done when: same gate.
- [ ] U14-E5 hooks + remaining providers (~10 hooks): react-hooks return-shape `@typedef`s + providers (TonicProvider etc.). `.test-d.tsx` backfill OPTIONAL (decide with user). done when: types:build green across react + react-hooks, no dangling refs, zero snapshot churn.

- [ ] U1 DISCOVERY+DESIGN (yield for review) — produce: (a) the set of v3 tokens tonic-one v4 still uses; (b) the concrete re-expression map: tonic-ui colorStyle dark/light + flat colors → `_dark`/`_light` semantic tokens carrying v3 VALUES; (c) per-file engine-port task list (source→target) for U2–U10 with exact verify commands. Write to `docs/`. done when: docs/token-mapping.md + docs/port-plan.md exist and I approve.
- [x] U2 utils: resolveTheme + toCSSVariable → @tonic-ui/utils (MAIN barrel; PR note re public surface). 167 tests. Committed aed56d2c7e.
- [x] U4+U5: createTheme calls resolveTheme (attaches toColorMode + TONIC_THEME); useTheme returns toColorMode(colorMode).get(). theme.get WORKS (verified get('colors.gray:100')→'#151515'). Broad sweep ZERO churn. Committed 0699c5a940. constants.js added.
- [x] U6-U10: CSS-vars API aligned. getter gates on theme.useCSSVariables + __colorMode shape resolution (dormant over flat v3) with legacy colon→hyphen path preserving existing cssVariables byte-for-byte; Box injects __colorMode (new react-base/src/internal/color-mode/context.js); TonicProvider/ThemeProvider useCSSVariables prop; CSSVariables [data-color-scheme] (omitted over flat v3); Dark/Light/InvertedMode set data-color-scheme. styled-system 144/144; ZERO react component snapshot churn (only Box.test.js.snap hash changed — that test renders useCSSVariables:true, expected). NOTE for PR: two var-naming conventions now — legacy gray-100 (cssVariables path) vs new gray_100 (useCSSVariables path). Committed ac436bc0a8.
- [x] U16 MFE-v3 fix: adapted both MFE GlobalStyles _component.scrollbar.* → v3 colon tokens (gray:70/30 track, white/black:disabled+tertiary thumb). BROWSER-VERIFIED: both demos (Module Federation + Wujie) render, 0 JS exceptions, 0 console errors — theme.get crash GONE. build:mfe 8/8 + next build OK. (Root cause of earlier failure: stale dist; must rebuild all 5 pkgs.) Committed b200a8a58d.
- [x] U13 JSDoc/.d.ts DISCOVERY: docs/jsdoc-dts-plan.md written. Pipeline = S+S+M (tsc --allowJs --emitDeclarationOnly → rollup dts() + global.d.ts ForwardRefComponent/StyledFC/StyleProps/ThemeScales). Annotation = L (~50-60 components + ~10 hooks; tonic-ui has ~3 vs tonic-one 153). Pipeline-only increment viable (tsc emits .d.ts per .js regardless of JSDoc).
- DECISION (user 2026-06-27): PIPELINE-ONLY now; DEFER the L per-component annotation to a later tranche/phase. Goal "generate .d.ts" delivered by pipeline.
- DECISION (user 2026-06-27, later): RE-OPEN the deferred U14 annotation (Sub-unit E). Source = tonic-one (state file moved here from tonic-one; SOURCE repo at /home/cheton/Code/trend-common-platform/tonic-one HEAD 70d2e78de). Mechanism: tonic-ui component bodies are near-identical to tonic-one's, so porting = faithfully copy the `@typedef XxxProps` + `@type {ForwardRefComponent<...>}` JSDoc blocks from the matching tonic-one file, ADAPTING to tonic-ui's actual props/variants/defaults (v3 may diverge from tonic-one's v4). Coverage today: tonic-ui 2 annotated (list/List.js, list/ListItem.js); tonic-one 153. Decomposed into tranches U14-E1..E5 below (Sub-unit E of docs/jsdoc-dts-plan.md). Posture stays supervised; yield on any checker fail or props that can't be cleanly mapped.
- [x] U14a PIPELINE PILOT on react-base: PASS from clean. Solution to .js-entry: typed src/index.ts barrel (re-exports ./box; allowJs pulls .js transitively). Added tsconfig.base.json, react-base/{tsconfig.json, global.d.ts (StyleProps/ThemeScales), src/index.ts, rollup dts output, package.json types+devDeps(typescript,rollup-plugin-dts)+scripts, .gitignore /@types}. Box annotated → real type emitted. NOT YET COMMITTED (fix idempotency first).
- DEFECT to fix in rollout: `yarn build` NOT idempotent — 2nd consecutive build fails TS2688/TS5055 because local "@types" typeRoot feeds tsc output back as input. Fix: drop local "@types" from typeRoots (point rollup dts input at @types/index.d.ts directly) OR prepend a clean step to types:build. Must be idempotent for CI.
- [x] U14b dts rollout: ALL 7 packages emit dist/index.d.ts. Idempotency fixed (del @types && tsc; removed bad @types typeRoot). react tsc over ~370 files exits 0 (loose any-heavy types, expected). JS builds intact. yarn.lock updated (typescript + rollup-plugin-dts devDeps — MUST be committed for CI). Committed 44a0506793 (U14a+U14b together, 40 files). Per-package: typed src/index.ts barrel + tsconfig + rollup dts output + package.json types wiring. PR NOTE: src/ now has BOTH index.js (runtime) and index.ts (dts input) — verified no JS-build confusion.
- [x] U15 theming docs: updated 3 pages (getting-started/usage, customization/css-theme-variables, components/color-style/useColorStyle) — document theme.get + useCSSVariables/--tonic over flat v3; ZERO v4-token/semantic-switching leakage (verified). colorStyle still the dark/light mechanism. Committed 3299680deb.
- [x] U11 full integration: PASS — 114 suites/756 tests/44 snapshots green, 0 lint errors, all 7 pkgs build incl dts (idempotent), docs build 165 pages + 8 MFE. 3 fixups applied + committed 425041e761 (dedupe imports in getter.js + TonicProvider.js; add CSSVariables to react expected-exports test).
- [x] U17 version config: COMMITTED be9b9caa5c (feat/semantic-token-css-variables). Header inlining defect FIXED + verified (switcher URLs built from inlined literals base+label; prerelease via literal TONIC_UI_PRERELEASE_LABELS="v3"; lint clean; built _app bundle confirmed to carry v1/v2/v3 URLs).
- v3 PRERELEASE READY (uncommitted in tonic-ui-main, NOT pushed, NOT committed — per user "do not push" / "do not commit to main directly"): 7 public pkgs = 3.0.0-alpha.0; pre.json mode=pre tag=alpha changesets=[tonic-ui-v3-alpha] ONLY; react CHANGELOG v3-only (the 6 v2 changesets were DELETED — they belong to v2's release line, NOT folded — per user catch). config.json restored to @tonic-ui/changelog-github. Used changelog-git generator for the version run (no GH token; user said ignore changelog for now). Added version:0.0.0 to react-docs + changelog-github (versionless → changeset crash). 25 files uncommitted in working tree.
- [x] changeset config baseBranch v2→main (amended into #1167, force-pushed → 997787768a).
- [x] PRERELEASE LANDED AS PR: #1167 (chore/enter-v3-prerelease → main). origin/main MOVED to 6df0d9f752 (= v2 release #1158 [react→2.15.0, consumed the 6 v2 changesets] + 90f4525a CI + #1166/#1161/#1160). Redid the prerelease on 6df0d9f752 base (clean: no v2 changesets to drop). 7 pkgs → 3.0.0-alpha.0, pre.json [tonic-ui-v3-alpha] only, v3-only changelog. Pushed branch + PR (user chose "own PR branch→main"; this is the PR mechanism, not a direct main push). Local main fast-forwarded to 6df0d9f752.
- U12 PLAN UPDATED: Phase 2 (feat/semantic-token-css-variables) must rebase onto the NEW base. User: Phase 2 "stacks on top" of the prerelease → rebase onto chore/enter-v3-prerelease (3.0.0-alpha.0). DROP redundant commits now in main: e40a3708 ButtonBox(#1161), 73f3e75 Portal + 191477e docs-sync (#1166). EXPECT CONFLICTS: package.json versions (Phase2 @2.14.0 vs base @3.0.0-alpha.0), sidebar-routes.js + llms.txt (base has merged Phase-1 versions), possibly env consumer files. Resolve favoring base (3.0.0-alpha.0 versions; main's nav) + keep Phase 2's MFE/engine/dts/docs/version-config additions. Then push + Phase 2 PR → main (stacked on #1167).
- (prior) v3 PRERELEASE SETUP on main (tonic-ui-main worktree). Decisions: tag=alpha; directly on main; all 7 public @tonic-ui/* packages major→3.0.0-alpha.0; the 6 pending changesets FOLD into 3.0.0-alpha.0 (changeset default; v3 supersedes v2). Steps: yarn install + write v3 major changeset (7 pkgs) + changeset pre enter alpha + changeset version → 3.0.0-alpha.0. Agent does setup (NO push); I review versions + push main. Then U12 rebase Phase 2 onto main (note: main now at 3.0.0-alpha.0 → package.json version conflicts possible during rebase).
- (was) U17 version config: config+ci-branch+env-loop+versions-template all PASS (env parity exact: v1/v2 byte-identical, v0 gone, v3 added; ci-branch resolves v2; build green). DEFECT: Header.js used DYNAMIC process.env[computedKey] → Next can't inline → empty switcher URLs + no prerelease badge at runtime. FIX IN PROGRESS (wm428gud6): construct URLs from literal base+label (mirror tonic-one Header) + bake literal TONIC_UI_PRERELEASE_LABELS + fix 2 jsx-no-leaked-render warnings. U17 NOT YET COMMITTED (commit after fix verified green via built-bundle grep).
- NEW UNIT U17 (user 2026-06-27): implement `tonic-ui-version.config.js` mirroring tonic-one's (real source = PR #402 "centralize doc version mgmt"; #403 = v4-stg merge). INCLUDES .github/workflows + react-docs changes. Plan: docs/version-config-plan.md. Discover/port from LOCAL tonic-one repo (adc URLs need EMU auth). Edits .github/workflows + react-docs → run AFTER U11.
- U17 VERSION SET DECISION (user 2026-06-27): config.versions = [ {branch:'main', label:'v3', prerelease:true}, {branch:'v2', label:'v2', current:true}, {branch:'v1', label:'v1'} ]. DROP v0 (and its @trendmicro/react-styled-ui scope — simplifies tag awk). v2 = current/stable (ci-branch builds v2, path /tonic-ui/react/v2); v3 = prerelease on main.
  IMPLICATIONS vs plan (plan assumed v0/v1/v2 unchanged): (1) version set CHANGES → switcher/versions-page now v1/v2/v3 (drop v0 row, ADD v3 row in the versions template). (2) v3 IS prerelease → DO port the prerelease/<sup>Preview</sup> badge handling (plan had said skip it). (3) tagPrefix: v3 @tonic-ui/react@3.*, v2 @2.*, v1 @1.* (v3 may match no tags yet — fine). (4) ci-branch trigger stays hardcoded v2 (config current=v2); making main→v3 deploy is a follow-up. (5) keep tonic-ui gh-pages path react/<version>/ + tonic-ui env var names (_SOURCE_CODE, _RELEASE_NOTES, _TAGNAME, _RELEASE_VERSION).
- [x] main BRANCH CREATED + pushed: origin/main @ c8dc661334 (= origin/v2 tip, includes Phase 1 #1166 Portal/env + ButtonBox #1161). This is Tonic UI v3's base. Worktree tonic-ui-main created on main for the Phase 2 rebase/PR job (main checkout stays on feat/semantic-token-css-variables for U17).
- version config note (user): tonic-ui-version.config.js will be added to v1 + v2 branches LATER (separate effort) — U17 only adds it to the v3/main line.
- U12 PLAN (after U17 commits): in tonic-ui-main worktree, rebase Phase 2 work onto main. Approach: `git rebase --onto main 191477e33b feat/semantic-token-css-variables` — replays the Phase-2-only commits (136a08dd MFE → U2 → U4U5 → U6-10 → U16 → U14 → U15 → U11fix(425041e) → U17) onto main, DROPPING the redundant Phase-1 commits (e40a3708 buttonbox, 73f3e75 Portal, 191477e docs-sync — all already in main via #1166). EXPECT CONFLICTS on sidebar-routes.js + llms.txt (136a08dd's old MFE versions vs main's Phase-1-merged versions): RESOLVE by keeping main's color-mode/color-style/env entries and ADDING only the micro-frontend route/entry on top. Then push + open Phase 2 PR targeting main.
- [x] U12 REBASE DONE: feat/semantic-token-css-variables rebased --onto chore/enter-v3-prerelease 191477e. 9 Phase-2 commits on the 3.0.0-alpha.0 base (tip da482d087e); version inherited 3.0.0-alpha.0; dropped ButtonBox/Portal/docs-sync (in main). Conflicts resolved: sidebar (kept main redirects + added Micro frontend), llms (base + only Micro frontend), yarn.lock (regenerated via yarn install, committed da482d087e). Branch clean, NOT pushed, NO PR (per user).
- ADVISABLE before PR: RE-RUN integration on the NEW base — U11 ran on the OLD v2 base; the new base (6df0d9f752) has newer v2 (List #1160, ButtonBox #1161, slots migration, date-fns v4) so the engine changes (utils/styled-system/react-base/react theme) should be re-verified (build all + test + docs build) to catch base-divergence before opening the Phase 2 PR.
- (was) U12 PHASE 2 PR: ON HOLD per user ("do not open pr now"). Phase 2 fully implemented + committed on feat/semantic-token-css-variables (local only, NOT pushed). When user gives go: rebase --onto chore/enter-v3-prerelease 191477e feat/semantic-token-css-variables (drop ButtonBox #1161 + Portal/docs-sync #1166; resolve sidebar/llms/package.json conflicts), push, open PR → main. Do NOT push/PR until told.
- REMAINING: (await user) U12 Phase 2 rebase + PR. DEFERRED: U14-annotation (L).
- Phase 2 commits on feat/semantic-token-css-variables: aed56d2c(U2) 0699c5a(U4U5) ac436bc(U6-10) b200a8a(U16) 44a0506(U14 dts) 3299680(U15 docs) + base MFE 136a08dd.
- [ ] U3 theme tokens: REVISED — do NOT re-express colorStyle into semantic tokens. Keep v3 flat colon tokens + colorStyle UNTOUCHED. Only ensure the theme still builds and the engine (resolveTheme/toColorMode) is a safe pass-through over flat v3 tokens (no `{ref}`/`_dark/_light` to resolve). The 10 TOKEN DECISIONS + token-mapping.md re-expression are SHELVED for the future v4 semantic-token integration (NOT this phase). done when: theme builds unchanged; resolveTheme(theme).toColorMode('dark'/'light') returns the flat v3 theme with a working `.get()`.
- [ ] U4 react createTheme + css-vars: `createTheme` calls `resolveTheme` + `mapThemeToCSSVariables` (emit `-dark`/`-light` + base alias); add `cssVariablePrefix`/`rootSelector`/`toColorMode`. done when: createTheme output resolved + has toColorMode + cssVariables w/ dark+light; tests.
- [ ] U5 react useTheme: return `theme.toColorMode(colorMode)` + `.get()` helper. done when: useTheme returns mode-resolved theme; tests.
- [ ] U6 styled-system getter: gate on `useCSSVariables`; resolve `_dark`/`_light`/`main`/`value` via `props.__colorMode`. done when: styled-system tests green; getter resolves semantic tokens per mode + emits var() when useCSSVariables.
- [ ] U7 react-base Box: inject `__colorMode` from ColorModeContext (stripped from DOM). done when: react-base tests; Box forwards __colorMode.
- [ ] U8 CSSVariables component: emit `-dark`/`-light` vars + `[data-color-scheme=dark|light]` overrides at rootSelector. done when: snapshot reflects switching; tests.
- [ ] U9 ThemeProvider/TonicProvider: thread `useCSSVariables` prop. done when: provider tests; useCSSVariables toggles emission.
- [ ] U10 color-mode components: DarkMode/LightMode/InvertedMode set `data-color-scheme` on root. done when: tests verify attribute.
- [ ] U11 integration: full tonic-ui `yarn test` + `yarn lint` across react/react-base/styled-system/theme/utils; regen snapshots; fix fallout. done when: all green.
- [ ] U13 DISCOVERY (JSDoc/d.ts): map tonic-one's JSDoc typing system (ForwardRefComponent typedef pattern) + the JSDoc→.d.ts generation pipeline (build script/tooling, rollup/tsc config, @types output). Compare to tonic-ui (has none). Write docs/jsdoc-dts-plan.md.
- [ ] U14+ (JSDoc/d.ts): execute the d.ts generation pipeline + add JSDoc annotations per the U13 plan. done when: tonic-ui emits .d.ts for components; tsc/type-tests pass.
- [ ] U15 docs: rework tonic-ui's theming docs pages (theme / color-mode / color-style / css-variables / customization) to MATCH tonic-one (semantic tokens, new CSS-variables API, useCSSVariables). done when: theming docs reflect the new engine + build.
- [ ] U12 PR: open Phase 2 PR (stacked on Phase 1). done when: PR open, follows Phase 1 PR.

## Blocked
- (none)

## Log
- 2026-06-26 bootstrap: Phase 1 (env provider) faithful to 4a0e44dbc for in-scope SOURCE (later found docs/files are STALE — handled in the env-mfe loop); Portal committed 1d5a0ecdf8; Phase 2 branch created.
- 2026-06-26 U1 discovery workflow complete: wrote docs/token-mapping.md (+10 open questions) and docs/port-plan.md. AWAITING USER REVIEW of token-mapping open questions before code mutation (supervised gate).

## TOKEN DECISIONS (resolved 2026-06-27 — these OVERRIDE token-mapping.md where they conflict; U3 maker MUST follow these)
- Q1 KEY FORMAT = **keep v3 colon keys** (gray:100, white:primary) — NO colon→dot rename. Semantic _dark/_light tokens reference primitives as `{gray:100}` (flat-key lookup works); CSS var name → `--tonic-colors-gray_100`. Zero blast radius on existing color usage / colorStyle. token-mapping.md's colon→dot assumption is SUPERSEDED — use colon refs in the value pairs.
- Q2 = v3 token set ONLY; no v4 palette merged → no 10..100 vs 100..1000 collision (we keep v3 colon keys).
- Q3 = uniform shape OK: every semantic leaf is `{_dark,_light}` even when both legs are equal (keeps getter/engine path uniform).
- Q4 = keep colors.text as combined v3 set (foreground color.* + accent text.selection/highlight) under their existing v3 names; do NOT split.
- Q5 = background.inverse → cross-reference `{background.inverted}` (single source of truth, no duplicate literal).
- Q6 = status colors = flat single `{_dark,_light}` leaf (match v3's single-value scope), NOT v4's rich text/icon/border shape.
- Q7 = keep v3 shadow names (thin/medium/thick) + values; do NOT adopt v4 low/medium/high directional shape.
- Q8 = deprecated groups (severity, chart.classic) → emit PLAIN (keep values for back-compat, no console-warn proxy); flag as deprecated in a comment.
- Q9 = transparent/current stay flat primitives (not semantic leaves).
- Q10 = preserve v3 asymmetry verbatim (light background.primary = {white:emphasis}); it's the existing v3 value.
- PRINCIPLE: preserve v3 VALUES faithfully; restructure into v4-style semantic `{_dark,_light}` format only; keep v3 keys/names; uniform leaf shape; cross-ref to avoid duplicate literals.
- PRIMITIVE SHAPE (user 2026-06-27): v3 primitives stay FLAT strings (e.g. `red:60: '#...'`). Do NOT give primitives the v4 nested `{ main, lighten:{...}, darken:{...} }` shape — that nested structure is only adopted WHEN v4 tokens are integrated (future, not this phase). Semantic `_dark`/`_light` tokens reference the flat primitives via `{gray:100}`. (resolveTheme's `.main` extraction simply won't trigger since v3 primitives are plain strings.)

## Open questions (RESOLVED above — kept for reference)
- Q1 primitive key colon→dot (`gray:100`→`gray.100`) breaks direct lookups — dual-alias during migration?
- Q2 step-key collision: v3 `10..100` vs v4 `100..1000` — confirm no v4 palette merged / namespace v3 set.
- Q3 mode-invariant leaves duplicate value across _dark/_light — acceptable?
- Q4 `colors.text` mixes v3 foreground `color.*` + accent `text.selection/highlight` — split?
- Q5 `background.inverse` vs `inverted` — duplicate literal or cross-ref?
- Q6 status color placement/shape — flat leaf vs nested?
- Q7 shadow naming thin/medium/thick vs v4 low/medium/high.
- Q8 deprecated groups (severity, chart.classic) — drop / emit plain / reproduce warning proxy?
- Q9 transparent/current kept as flat primitives.
- Q10 light `background.primary` uses `white:emphasis` (not a gray step) — intentional asymmetry?

## Docs
- `docs/token-mapping.md` — the colorStyle→_dark/_light re-expression (v3 values) + 10 open questions
- `docs/port-plan.md` — per-unit (U2..U11) engine-port plan: source→target files, verify cmds, dep order
- `docs/jsdoc-dts-plan.md` — (pending U13) JSDoc typing + .d.ts generation plan
