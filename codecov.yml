coverage:
  # https://docs.codecov.com/docs/commit-status
  status:
    project:
      default:
        target: auto 
        threshold: 2%
    patch:
      default:
        target: auto 
        threshold: 2%
